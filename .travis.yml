language: groovy
jdk:
- oraclejdk8
before_install:
- chmod +x gradlew
- chmod +x travi-build.sh
before_script:
- rm -rf target
script: ./travis-build.sh
env:
  global:
  - GIT_NAME="Vitalii Samolovskikh"
  - GIT_EMAIL="kefirfromperm@gmail.com"

